<section class="page-title">
  <h1>Welcome, <%= @user.name %> to the Administration Dashboard!</h1>
</section>

<section class="admin-info">
  <p>Here is the place to add and update aspects of Quiet Joy Studio.</p>
</section>

<div class="quicknav-content">
  <%= link_to  'my art', admin_artworks_path %> / <%= link_to  'my garments', admin_clothings_path %> / <%= link_to  'my workshops', admin_classrooms_path %> / <%= link_to  'my collections', admin_collections_path %>
</div><br>
<div class="quicknav-content">
  <%= link_to  'all users', admin_users_path %> / <%= link_to  'my email list', admin_emails_path %> 
</div><br>


<div class="admin-dashboard-layout">
  <ul>
    <li>
      <section class="art-layout">
        <% @artworks.each do |artwork| %>
        <% if !artwork.image.nil? || !artwork.image.empty?%>
        <p class="art-image"><%= link_to image_tag(artwork.image), admin_artwork_path(artwork) %></p>
        <% else %>
        <p>No Image to display</p>
        <% end %>
        <% end %>
      </section>
      <section class="all-my-art-button">
        <%= button_to 'My Art', admin_artworks_path, method: :get  %>
      </section>
    </li>
    <li>
      <section class="garment-layout">
        <% @clothings.each do |clothing| %>
          <p class="garment-image"><%= link_to image_tag(clothing.image), admin_clothing_path(clothing) %></p>
        <% end %>
      </section>
      <section class="all-my-garments-button">
        <%= button_to 'My Garments', admin_clothings_path, method: :get %>
      </section>
    </li>
    <li>
      <section class="workshop-layout">
        <% @classrooms.each do |classroom| %>
            <p class="workshop-image"><%= link_to image_tag(classroom.image), admin_classroom_path(classroom) %></p>
        <% end %><br>
      </section>
      <section class="all-my-workshops-button">
        <%= button_to 'My Workshops', admin_classrooms_path, method: :get  %>
      </section>
    </li>
    <li>
      <!-- <section class="email-layout">
      </section> -->
    </li>
    <!-- <section class="email-list-button">
      <p><%= button_to 'Email List', admin_emails_path, method: :get  %></p>
    </section> -->
  </ul>

</div>
