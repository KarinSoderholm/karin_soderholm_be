<section class="page-title">
  <h1>Edit Artwork Statement:</h1>
</section>

<div class="quicknav-content">
  <%= link_to  'admin dashboard', admin_dashboard_index_path %> / <%= link_to "#{@artwork.name}'s artwork statment", "/admin/art/#{@artwork.id}/artwork_statement/#{@artwork_statement.id}"%>/ <%= link_to "#{@artwork.name}", admin_artwork_path %>
</div><br>

<%= form_for [@artwork_statement], url: "/admin/art/#{@artwork.id}/artwork_statement/#{@artwork_statement.id}/edit", method: :post do |f| %>
  <%= f.label :statement %><br>
  <%= f.text_field :statement %><br>

  <%= f.submit 'Update Artwork Statement' %>
<% end %>
