<section class="page-title">
  <h1>All my orders</h1>
</section>
<% @orders.each do |order| %>
  <section id="order-<%= order.id %>">
    <%= link_to order.id, "/profile/orders/#{order.id}" %>
    <p>Created On: <%= order.created_at.localtime.strftime('%m/%d/%y') %></p>
    <p>Updated On: <%= order.created_at.localtime.strftime('%m/%d/%y') %></p>
    <%# <% binding.pry %>
    <p>Status: <%= order.status %></p>
    <%# <!-- <p><%= order.count_of_items %>
    <%# <p>Total: <%= number_to_currency(order.grand_total) %>
  </section>
<% end %>
